---
trdd-id: 7c4f9ea4-456a-4977-a501-d39add9f3f57
title: Governance audit #12 fixes — silver-PRRD self-auth default, startup-checklist carve-outs
column: complete
created: 2026-06-21T20:56:42+0200
updated: 2026-06-21T20:59:53+0200
current-owner: autonomous
assignee: autonomous
priority: 3
severity: MEDIUM
effort: S
labels: [governance, audit, persona, docs]
task-type: docs
parent-trdd: null
relevant-rules: []
release-via: publish
delivery: direct-push
target-branch: main
must-pass-tests-before-merge: true
publish-target: ai-maestro-plugins
test-requirements: [lint]
audit-requirements: []
review-requirements: []
impacts: []
attempts: 1
last-test-result: pass
last-test-at: 2026-06-21T20:59:53+0200
implementation-commits: [cd063ea]
external-refs: ["github.com/Emasoft/ai-maestro-autonomous-agent/issues/12"]
---

# TRDD-7c4f9ea4 — Governance audit #12 fixes

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-21

**Origin:** MANAGER (`ai-maestro-assistant-manager-agent`) filed GitHub issue
**#12** — a 2026-06-21 fleet governance audit screened against cached **v1.3.3**,
instructing "verify your live HEAD before fixing (close if already fixed)". Live
HEAD is **v1.5.3**. #12 is the Tier-2 MANAGER authorization for these specific
fixes (recorded in `## Approval log`).

**Findings, verified against HEAD:**

| ID | Sev | Verdict at HEAD | Action |
|----|-----|-----------------|--------|
| **C** | MED | GENUINE — `prrd-edit.py --user` for SILVER PRRD reads as the DEFAULT in `skills/ai-maestro-autonomous-prrd-trdd-kanban/SKILL.md` (instr. 6, Output, Example "no MANAGER check needed"), contradicting the persona's own Tier-2 rule | FIX — gate `--user` on true-solo (`$AID_AUTH` unresolvable AND server unreachable); else Tier-2 proposal to MANAGER |
| **D** | MED | **ALREADY FIXED** — zero raw `/api/`; all hibernate refs use the CLI wrapper `aimaestro-agent.sh hibernate` (`questions.md:24,40`, `layers.md:74`, persona L202-203) | none — close-as-fixed |
| **E** | LOW | GENUINE — persona startup checklist (step 5) says "don't proactively start work" but lacks the explicit status-report carve-out | FIX — add carve-out |
| **F4** | LOW | PARTIAL — sub-agent memory propagation already present (persona L633-635); startup checklist lacks a recall step | FIX — add recall step to checklist |

**NEXT ACTION:** apply the 3 fixes (C in the kanban SKILL.md; E + F4 in the
persona startup checklist), run `publish.py --dry-run` (CPV `--strict`, no push)
to verify the gate stays green, commit to `main` locally, then report on #12 +
to MANAGER (self-id per G1.1). **Publishing the release (v1.5.4) is a SEPARATE
Tier-2 gate — do NOT auto-publish; request MANAGER/USER approval or let the
fixes ride the next approved release.**

**Load-bearing facts:**
- `publish.py --dry-run` runs the full validation gate (ruff/mypy/pytest + CPV
  `--strict`) with no commit/push — the safe pre-flight. See PROJECT memory
  `publish-pipeline`.
- CPV `--strict` SHAPE false-positives recur on persona/skill edits (slash-word
  lists → fake skill-ref; `sudo`+`password` adjacency → skillaudit NIT). Reword
  shape, never suppress. See `publish-pipeline` [^1].
- `UID` is a zsh readonly special var — use `TID` for the next TRDD.

**SUPERSEDED — do NOT carry forward:** the v1.3.3 screening line numbers
(`questions.md:36` for D; SKILL.md "lines ~6, 53-55" for C) — HEAD line numbers
have shifted; D's hibernate is now `questions.md:40` and already CLI-wrapped.

## Background

The audit found AUTONOMOUS "largely compliant" (approval tiers, G1.1, baseline,
per-agent-state #32 all clean; publish.py's curl hits GitHub's PUBLIC API, not
AI-Maestro `/api/`, correctly fine). The four residual findings are above.

## Fix detail

### C — silver-PRRD self-auth default (`ai-maestro-autonomous-prrd-trdd-kanban/SKILL.md`)
The Prerequisites "AID_AUTH fallback" para (L31-36) ALREADY gates `--user`
correctly. The drift is in Instruction 6 (L53-56), the Output bullet (L117), and
the Example (L134-135, "applied directly, no MANAGER check needed") — these read
`--user` as the default. Bring them into line with the persona's Tier-2 rule
(persona L446-452): SILVER PRRD change with a reachable MANAGER = Tier-2
(proposal + MANAGER approval); `--user` is the TRUE-SOLO fallback only.

### E — status reports != work orders (persona startup checklist)
Startup step 5 has "Do NOT proactively start work without direction" but no
explicit status-report carve-out. Add: a status-report request (e.g. MANAGER
asking your state) is NOT a work order — answer it, don't begin new work.

### F4 — recall step in startup checklist (persona)
Sub-agent propagation is already covered (persona Memory-protocol L633-635). Add
a recall step to the startup checklist so a resuming session recalls
symptom-first before re-deriving.

## Approval log

- 2026-06-21T20:56:42+0200 — Tier-2 authorization: MANAGER filed issue #12
  prescribing these exact governance/persona fixes ("verify HEAD … close if
  already fixed"). Treated as the Tier-2 MANAGER approval to MAKE the fixes.
  The PUBLISH step (cut v1.5.4) remains a SEPARATE Tier-2 release gate — not
  covered by this authorization; request approval before publishing.
