---
trdd-id: V1AGFGQK
title: Sync guidance to Claude Code 2.1.233
column: dev
created: 2026-08-15T00:53:38+0200
updated: 2026-08-15T00:53:38+0200
current-owner: autonomous-session
task-type: docs
release-via: publish
approval-tier: 0
---

# Sync guidance to Claude Code 2.1.233

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-08-15

- Window: **2.1.233 only** (predecessor TRDD-GA3TCRC7 covered 2.1.225→2.1.232; contiguous).
- Host measured at 2.1.233 (`claude --version`, 2026-08-15). Changelog entry published
  upstream ~2026-08-15 (it lagged the binary by hours — the sweep was blocked on it).
- **NEXT ACTION:** fix README input-redirection claim (below), advance the
  `[[claude-code-version-sync]]` pointer to 2.1.234, commit, publish patch.

## Sweep findings (full 2.1.233 changelog reviewed line-by-line)

**One shipped claim falsified:**

- `README.md` ~L211: "Since 2.1.232 plain input redirection (`< file`) is
  permission-checked on every platform … the redirect is no longer the quiet way in."
  **2.1.233 REVERTED the 2.1.232 Bash permission changes for input redirections**
  (and Cygwin-style symlinks on Windows); upstream says "a narrower version will
  return in a later release". Fix: rewrite the bullet to record the revert and keep
  the durable advice (keep commands short) without asserting the check exists.

**Checked and NOT affected (recording the rejects so the pass is auditable):**

- Todo/task-tool removal on Fable 5/Sonnet 5/Opus 4.8+ (`CLAUDE_CODE_ENABLE_TODO_TOOLS=1`
  restores): zero `TaskCreate`/`TodoWrite`/todo-list references in any shipped surface
  (agents/, skills/, rules/, docs/, README) — nothing to fix.
- 2.1.232 Cygwin-symlink claim: exists only inside the terminal-frozen
  TRDD-GA3TCRC7 archive body — historical record, exempt from edits by design.
- MCP v2 stream fix, cgroup limit, WebFetch TTL var, GitLab MR worktrees, apps-gateway
  identity forwarding, screen-reader/effort UI, self-hosted-runner start time,
  `\??\` NTLM fix: no shipped claim in this repo touches any of them.

## Acceptance

- [ ] README bullet no longer asserts the reverted permission check
- [ ] version-sync memory pointer advanced to 2.1.234 in the same change
- [ ] suite green, publish gate clean, patch released

implementation-commits: []
