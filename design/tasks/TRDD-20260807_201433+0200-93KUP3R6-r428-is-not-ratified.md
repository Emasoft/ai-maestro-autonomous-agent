---
trdd-id: 93KUP3R6
title: R42.8 is not ratified - revert the persona and fixtures to R42.1-R42.7
column: complete
created: 2026-08-07T20:14:33+0200
updated: 2026-08-07T20:14:33+0200
current-owner: ai-maestro-autonomous-agent
task-type: security
scope: project
relevant-rules: [1]
supersedes-commit: 3cc4bff
implementation-commits: []
external-refs: [ai-maestro#125, ai-maestro#128, ai-maestro#129]
---

# TRDD-93KUP3R6 — R42.8 is NOT ratified

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-08-07

**DONE.** 119 tests pass. Reverts the governance substance of `3cc4bff`.
**If you find any surviving text treating R42.8 as a rule, it is a regression — the
probe below is the arbiter, not this file.**

## The fact, verified here, not accepted from a peer

CORE messaged this session retracting a ruling it had given me on `ai-maestro#128`.
I verified before changing anything — the retraction is correct:

```
gh issue view 125 --repo Emasoft/ai-maestro
  state = OPEN   created 2026-08-05   closed: never
  title = "R42 amendment REQUEST (MAESTRO): grant MANAGER and CHIEF-OF-STAFF ..."

gh api "repos/Emasoft/ai-maestro/contents/docs/GOVERNANCE-RULES.md?ref=governance-rules"
  1929 lines · 14 "R42" hits
  subsections : R42.1 R42.2 R42.3 R42.4 R42.5 R42.6 R42.7
  "R42.8"     : 0 occurrences
```

**The grep is positively controlled** — it finds R42.1–R42.7 in the same pass, so the
zero is a real absence rather than a wrong path or a dead probe. Two traps that have
caught others: the path is `docs/GOVERNANCE-RULES.md` (repo root returns an empty file
whose zero is indistinguishable from a real absence), and the content lives on the
unmerged **`governance-rules`** branch (a query against `main` 404s, and that 404 means
nothing).

**The issue cited AS the grant is the request, still open.**

## What was wrong here

`3cc4bff` (this morning) rewrote FORBIDDEN ACTION 6 to teach R42.8 as *"ratified by the
USER 2026-08-05"*, title-scoped and verb-scoped. It was wrong in substance. Worse, the
guard test asserted:

```python
assert "R42.8" in text, "persona must acknowledge the ratified R42.8 unblock exception"
```

**The suite was defending the error.** That is the "a green test can pin the DEFECT"
failure — the correction reddens the suite before it fixes anything, and the natural
instinct is to revert the correction. Here the pinned defect was not a code bug but an
**unverified external governance claim encoded as a contract.**

## What did NOT change: the verdict

Every refusal this plugin would give is unchanged. `inject` / `slash` / `queue` stay
SELF-ONLY for every title **under the ratified rules and under the proposal**, because
they deliver an arbitrary command and so express the CALLER's decision (R42.1). So the
answer never moved — only the stated REASON was false.

That is the whole hazard: **a correct answer resting on a false ground passes every test
that checks the answer, and so is never re-examined.** It is why the persona and ADV-03
now carry the probe instead of a citation.

## The asymmetry that decides the default

Erring toward the ratified text costs **at most a stalled agent**. Erring toward the
proposal means performing a cross-agent act on someone else's session that **no rule
permits**. Not symmetric, so "revert to ratified" is the correct default, not the timid
one.

## Changes

- **persona** — states the ratified position (R42 admits no cross-agent exception for any
  title); presents R42.8 as a PENDING request with `#125` OPEN; instructs the reader to
  re-run the probe rather than believe a cited authority *or the paragraph itself*.
- **`tests/scenarios/adversarial-fixtures.md`** — ADV-03 rewritten on two grounds that do
  not depend on `#125`'s outcome, plus a HISTORY note so a future reader does not
  "restore" the old wording; coverage-map row updated.
- **guard** — inverted, then **falsified four ways**, each independently:

  | broke | result |
  |---|---|
  | `PENDING amendment REQUEST` | 1 failed |
  | `which is **OPEN**` | 1 failed |
  | `R42 admits NO cross-agent exception for any title` | 1 failed |
  | re-introduced a `(ratified` claim | 1 failed |

  The last one matters most: it is a NEGATIVE assertion, the kind most prone to passing
  vacuously, so it was proven to fire.

## Outward actions

- **`ai-maestro#129` — which I opened — annotated AND retitled** to
  `[RETRACTED — premise false, see latest comment] …`. The title was the propagation
  surface; a correct comment under a wrong title fixes little, since people scan titles.
- CORE answered directly, with the correction, my disagreement about fault (trusting a
  peer is fine; industrialising an unverified claim into a fleet grep and a test is mine),
  and an explicit note that I did **not** post on CORE's behalf.
- USER-scope memory corrected by supersession — nothing deleted.

## Deliberately NOT done

- **No claim about third-party state.** CORE reported MAINTAINER `e2394a7` and MANAGER
  `amama-agent-unblock` v2.15.0; I have not verified either and do not assert them.
- **No fleet-wide sweep** for surviving R42.8 citations — offered to CORE, not run
  unasked; it reads other projects' trees.

## Verification

- `uv run pytest -q` → **119 passed**.
- `uv run python scripts/publish.py --gate` → exit 0, CRITICAL=0 MAJOR=0 MINOR=0 NIT=0.
- `git grep -nI 'R42\.8'` → every surviving hit is a correction, none asserts ratification.
