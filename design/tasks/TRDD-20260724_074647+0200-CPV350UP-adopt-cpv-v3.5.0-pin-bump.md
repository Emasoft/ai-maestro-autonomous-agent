---
trdd-id: CPV350UP
title: Adopt CPV v3.5.0 across the pipeline (lockstep gate-pin bump from v3.2.0)
column: dev
created: 2026-07-24T07:46:47+0200
task-type: infra
current-owner: ai-maestro-autonomous-agent
relevant-rules: []
parent-trdd:
npt: []
eht: []
implementation-commits: []
updated: 2026-07-24T07:46:47+0200
---

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-07-24

- **DONE:** all 5 CPV gate sites bumped `@v3.2.0` → `@v3.5.0` in lockstep
  (`.github/workflows/ci.yml` gate+comment, `.github/workflows/release.yml`
  gate+comment, `scripts/publish.py` Steps 4/5/5.5). Prose comments +
  TRDD-ref chain updated. Parity test stays green (version-agnostic).
- **PROOF (all @v3.5.0, working tree):** `plugin . --strict` → `✓ All checks
  passed, CRITICAL=0 MAJOR=0 MINOR=0 NIT=0 WARNING=3`; `lint .` → EXIT=0 all
  zero; `ci-preflight .` → PARITY-CLEAN FAIL=0 PASS=8 WARNING=3(absent-tool);
  `pytest tests/ -q` → 97 passed.
- **NEXT ACTION:** commit pipeline files + this TRDD by name; run
  `python scripts/publish.py --gate` on the clean tree for end-to-end proof;
  flip `column: complete` and record the impl SHA in `implementation-commits`.
- **SUPERSEDED — do NOT carry forward:** the earlier session assumption that
  "latest CPV = v3.2.0" — CPV shipped v3.2.1/v3.3.0/v3.4.0/v3.5.0 on
  2026-07-23; v3.5.0 is `Latest`.
- **DO-NOT-TOUCH guardrails held:** the 3 declared `intentional_divergence`
  files (`scripts/publish.py` custom logic, `cliff.toml`, `git-hooks/pre-push`)
  were NEVER force-templated; the hook divergence (TRDD-HOOKDIVG) was not
  repointed. No publish, no push.

## Context — why this task exists

CPV canon moved. At session start the pipeline was pinned `@v3.2.0` (adopted by
TRDD-CPV320UP). Empirically verified via `gh release list`: CPV published
v3.2.1, v3.3.0, v3.4.0, and **v3.5.0** (`Latest`, 2026-07-23T20:50:02Z) — the
"latest = v3.2.0" premise was stale. "Update the pipeline to the latest CPV
canonical standard" therefore means a lockstep bump v3.2.0 → v3.5.0.

Changes across the span, and their bearing on THIS plugin:
- v3.2.1 — skillaudit CMD_INJECTION FN scoping fix (no effect here).
- v3.3.0 — NEW detector RC-165 (dependency agent-context-writer) + npm
  semver-range FP fix. **Did NOT fire** on this plugin (0 MAJOR/MINOR).
- v3.4.0 — canon change "lint-only: CPV recommends linters, never formatters".
  Added **no** new blocking pipeline-drift here.
- v3.5.0 — adds `/cpv-agent` slash command (no bearing on the gate).

Because the v3.5.0 `--strict` run is clean (0 blocking; same 3 by-design
RC-PIPELINE-DRIFT-001 ahead-of-canon WARNINGs on ci/release/notify-marketplace),
the bump is the ONLY correct change — the pipeline is already structurally
at/ahead of canon and must NOT be force-templated.

## The change (atomic)

Bump `git+https://github.com/Emasoft/claude-plugins-validation@v3.2.0` →
`@v3.5.0` at the 5 executable gate sites (2 workflows + publish.py Steps
4/5/5.5), plus the two stale prose pins (ci.yml comment) and the TRDD-ref
chain in the ci.yml / release.yml gate comments. `tests/test_cpv_pin_parity.py`
is version-agnostic and enforces that all 5 sites stay in lockstep.

## Verification

- `pytest tests/ -q` → 97 passed (incl. the 3 pin-parity tests).
- CPV @v3.5.0 `plugin --strict` / `lint` / `ci-preflight` on the working tree —
  all pass (see STATE PROOF).
- `python scripts/publish.py --gate` on the clean committed tree — EXIT=0.

## Approval log
- 2026-07-24T07:46:47+0200 — Tier-0 in-scope pipeline maintenance (version-pin
  bump to current canon, no baseline deviation, reversible). Authored + executed
  by the sole worker; no external approval required.
