---
trdd-id: fc155c40-d52d-4b58-8b47-ea13ccf6ebd3
title: Decouple executable /api/* references → frozen aimaestro-*.sh CLI verbs (R23)
column: published
approval-tier: 2
created: 2026-06-18T23:19:49+0200
updated: 2026-06-18T23:25:50+0200
published-version: 1.4.1
published-at: 2026-06-18T23:24:16+0200
implementation-commits: [2d27167, f1003db]
current-owner: aimaa
assignee: aimaa
priority: 3
severity: MEDIUM
effort: S
labels: [governance, decoupling, frozen-interface, fleet-readiness]
task-type: refactor
parent-trdd: null
relevant-rules: [1]
release-via: publish
delivery: direct-push
target-branch: main
test-requirements: [lint, typecheck, unit]
review-requirements: []
impacts: [public-api]
external-refs: ["github.com/Emasoft/ai-maestro-autonomous-agent/issues/8", "Emasoft/ai-maestro#36", "GOVERNANCE-RULES.md R23"]
---

# TRDD-fc155c40 — Decouple executable /api/* → frozen CLI verbs (issue #8, R23)

## Context

Fleet-readiness task from the MANAGER (autonomous-agent issue #8), under the
USER's standing fleet pre-authorization (2026-06-15). Keystone `Emasoft/ai-maestro#36`
(the immutable CLI layer in `~/.local/bin`, commit `da9f3721`) is DEPLOYED — the
deploy signal `[ -e ~/.local/bin/aimaestro-teams.sh ]` is PRESENT. The governance
rule is **R23** (frozen-interface) in `GOVERNANCE-RULES.md`.

**Rule:** every *executable* / agent-facing ai-maestro **server** `/api/*` call
site (and every prescription to "use the HTTP API") is repointed to its frozen
CLI verb, because the server's HTTP routes can be renamed (that was #36) but the
`aimaestro-*.sh` CLI is the immutable interface. `gh` / `api.github.com` /
`crates.io` are OUT of scope (not the ai-maestro server). Inert doc/changelog
mentions are EXEMPT.

Peer precedent (all shipped post-#36-deploy, CPV `--strict` clean): maintainer
v1.6.0 · architect v2.8.1 · chief-of-staff · AMAMA v2.11.1.

## Deployed CLI verb surface (verified from script source, server-independent)

- `aimaestro-agent.sh`: list show resolve create delete **update** rename session
  **hibernate** wake restart skill **plugin** export import
- `aimaestro-teams.sh`: add-agent create delete kanban-config list remove-agent show update
- `aimaestro-governance.sh`: whoami requests request approve reject transfer

All ops this plugin references (agent config/title `update`, plugin install
`plugin`, `hibernate`, team state via `aimaestro-teams.sh`) HAVE deployed verbs →
**zero DECOUPLE-BLOCKED tags**.

## Full audit + classification (3 files; README EXEMPT)

| File:line | Old (server route / HTTP-API prose) | New (frozen CLI) | Class |
|---|---|---|---|
| main-agent.md:129-131 (Rule 2) | "AI Maestro HTTP API (`POST/PATCH/DELETE /api/agents`,`/api/teams`)" | `aimaestro-agent.sh` / `aimaestro-teams.sh` | executable instruction |
| main-agent.md:483 | "set via the AI Maestro PATCH API" | the agent CLI (`aimaestro-agent.sh update`) | prescription |
| main-agent.md:487 | "via `PATCH /api/agents/<your-id>`" | `aimaestro-agent.sh update <your-id>` | prescription |
| main-agent.md:490 | "through the PATCH /api/agents API" | through `aimaestro-agent.sh update` | refuse-script |
| questions.md:16 (Q3) | "Use the HTTP API instead." | "Use the AI Maestro CLI (`aimaestro-agent.sh`) instead." | prescription |
| questions.md:34 | "alternative via the HTTP API" | "alternative via the AI Maestro CLI" | prescription |
| questions.md:36 | "`POST /api/agents/<other-id>/hibernate`" | "`aimaestro-agent.sh hibernate <other-id>`" | agent-executable |
| layers.md:38 | "NO (use HTTP API)" | "NO (use `aimaestro-agent.sh`)" | table |
| layers.md:39 | "NO (use HTTP API)" | "NO (use `aimaestro-teams.sh`)" | table |
| layers.md:52 | "use the HTTP API or a different path" | "use the AI Maestro CLI or a different path" | code-example msg |
| layers.md:64 | "call `PATCH /api/agents/<my-id>`" (install plugin) | "run `aimaestro-agent.sh plugin` (manage your plugins)" | example |
| layers.md:74 | "MANAGER calls the hibernate API" | "MANAGER runs `aimaestro-agent.sh hibernate`" | example |
| layers.md:76 | "call `PATCH /api/agents/<my-id>`" (config) | "run `aimaestro-agent.sh update <my-id>`" | example |

EXEMPT (inert, unchanged): README.md:98/108 ("ChangeTitle pipeline auto-installs
it" — describes server behavior, no route, no agent prescription).

## Plan

1. Apply the 3-file edits above (Edit tool; re-read before each).
2. Verb-audit: `git grep '/api/[a-z]'` returns only `api.github.com`; no bare
   "use the HTTP API" prescriptions remain in persona/skills.
3. `publish.py --patch --dry-run` (ruff + mypy + pytest + CPV `--strict`).
4. `publish.py --patch` → decoupling release (v1.4.1). Ack issue #8.

## Approval log

- 2026-06-18T23:19:49+0200 — Authored `planned` (Tier-2, MANAGER-ordered fleet
  task under USER standing pre-auth 2026-06-15). Executing per "do NOT stop,
  complete to completion."
- 2026-06-18T23:25:50+0200 — COMPLETED + PUBLISHED v1.4.1 (`2d27167` feature +
  `f1003db` release). All gating CI green (Plugin Validation, Release, Notify
  Marketplace). 13 sites repointed across 3 files; zero DECOUPLE-BLOCKED tags
  (all verbs deployed). Acked + closed issue #8. Archived.
