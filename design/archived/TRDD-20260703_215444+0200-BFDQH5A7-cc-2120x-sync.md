---
trdd-id: BFDQH5A7
title: Sync plugin guidance to Claude Code 2.1.181-2.1.200 changelog
column: complete
created: 2026-07-03T21:54:44+0200
updated: 2026-07-03T21:54:44+0200
current-owner: ai-maestro-autonomous-agent
assignee: ai-maestro-autonomous-agent
priority: 4
severity: LOW
effort: S
labels: [docs, cc-sync, autonomy]
task-type: docs
parent-trdd: null
relevant-rules: []
release-via: none
test-requirements: []
review-requirements: []
impacts: []
attempts: 1
last-test-result: not-run
implementation-commits: [b2dd4d2]
external-refs: [https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md]
---

# TRDD-BFDQH5A7 — CC 2.1.181->2.1.200 sync

## Origin

USER: "update the plugin to be inline with the following recent claude code
changes" + the 2.1.181->2.1.200 changelog. Distilled it to the 10 behavior
CHANGES (not CC-internal bug fixes) that could make an autonomous-agent plugin
stale, then delegated a read-only triage (bg general-purpose agent, 189k tok).

## Finding

**0 stale/broken sites.** The plugin is structurally insulated:
- Ch1 AskUserQuestion-no-auto-continue — ABSENT: interaction is AMP async +
  drain-on-idle approval-tier queue, never `AskUserQuestion`.
- Ch2 permission-mode->Manual — ABSENT (no permission-mode flags).
- Ch4 /agents wizard removed — ABSENT (all `/agents` hits are FS paths).
- Ch5 rate-limit-as-string workaround now stale — ABSENT (that pattern lives in
  the global corpus-distillation rule, not this plugin).
- Ch7 Sonnet 5 default — `agents/...-main-agent.md:10` `model: sonnet` is a
  valid family ALIAS (now -> Sonnet 5), NOT a stale dated pin. Kept as-is.
- Ch8 launcher-msg != USER-approval — already CONSISTENT (persona:699).
- Ch9 scheduled/webhook notifications, Ch10 frontmatter casing — ABSENT.

Report: `reports/plugin-cc-sync/20260703_215104+0200-triage.md`.

## Change applied (commit b2dd4d2) — 2 on-mission additive alignments

1. **README `## Running unattended`** — documents `CLAUDE_CODE_RETRY_WATCHDOG`
   (CC 2.1.186/199): `CLAUDE_CODE_MAX_RETRIES` now caps at 15; the watchdog
   lifts that cap and defaults to 300 retries with backoff on transient
   non-usage-limit errors. This plugin's whole purpose is long UNATTENDED runs,
   so this is the highest-value alignment — a brief 5xx no longer ends the turn;
   paired with the janitor heartbeat (the usage-limit-reset wake trigger).
2. **persona `Propagate to sub-agents`** — notes sub-agents now run in the
   BACKGROUND by default (CC 2.1.198): dispatch independent work in parallel and
   act on completion notifications instead of blocking on one.

## Verification

Doc-only (no runtime/test surface). Env-var semantics cross-checked against the
verbatim changelog entries (2.1.186 cap-at-15 + use-watchdog; 2.1.199 watchdog
default 300 + lifts the 15 cap; 2.1.198 subagents-background-by-default).

## Not done (deliberate, per Don't-Over-Engineer)

`model: sonnet` left as-is (valid alias, cache-friendly). No speculative adds
for CC settings the plugin does not use (autoMode.classifyAllShell,
sandbox.credentials, respondToBashCommands). Not published — publish is gated
on USER approval (publish.py).
