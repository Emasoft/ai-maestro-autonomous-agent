---
trdd-id: ffcfadbd-58b1-4e48-a8c4-9089884684f0
title: Claude Code currency review + prune vestigial validators (v1.3.3)
column: completed
created: 2026-06-11T14:54:14+0200
updated: 2026-06-11T14:54:14+0200
current-owner: aimaa
assignee: aimaa
priority: 3
severity: LOW
effort: M
labels: [maintenance, claude-code-currency, cleanup, release]
task-type: refactor
parent-trdd: null
relevant-rules: [1]
release-via: publish
publish-target: ai-maestro-plugins
publish-channel: stable
test-requirements: [unit, lint, typecheck]
review-requirements: [human-review]
impacts: [ci-pipeline]
last-test-result: pass
implementation-commits: [a25d181, 0fff792]
published-version: 1.3.3
external-refs: ["https://code.claude.com/docs/en/changelog.md"]
---

# TRDD-ffcfadbd — Claude Code currency review + prune vestigial validators (v1.3.3)

> USER request: "read the github repo for open issues. Also bring the plugin up to date
> with the latest claude code changes." Open issues: 0. Currency review found the plugin
> already conformant with Claude Code 2.1.173; USER then chose (AskUserQuestion, option 4)
> to also prune the vestigial bundled validators. Authored as a `completed` archive record.

## Currency verdict (verified against changelog 2.1.173 + plugins-reference)

Already conformant — zero deprecated features. `plugin.json` clean (no top-level
`themes`/`monitors`); agent/skill frontmatter current; `hooks/hooks.json` empty (all
hook-contract changes N/A); no `commands/` (all slash-command changes N/A); CI + publish.py
both gate on the latest REMOTE `cpv-remote-validate`. New 2.1.144–2.1.173 features
(`defaultEnabled`, agent `effort:`, `isolation: worktree`, `disallowed-tools`,
`MessageDisplay`, `reloadSkills`, dynamic workflows) are optional/inapplicable to a
mandatory governance role-plugin.

## What changed (the two refinements)

1. **`plugin.json` dependencies → documented object form** `[{"name":"ai-maestro-plugin"}]`
   (no version pin — base plugin tracks latest). Cleared the lone CPV dep-pin WARNING
   (10→9 advisory warnings).
2. **Pruned 18 verified-vestigial scripts** (commit a25d181, −17,199 LOC): 16 `validate_*.py`
   + `lint_files.py` + the superseded `scripts/pre-push-hook.py`. Import-graph analysis
   proved nothing reachable imports/invokes them; `validate_scoring.py` even imported a
   non-existent `validate_plugin`. KEPT the load-bearing chain
   `publish.py → gitignore_filter → cpv_validation_common → smart_exec` + memory helpers.
   The real pre-push gate is `.githooks/pre-push` (process-ancestry, auto-generated by
   publish.py) — untouched.

## Verification

- Import-graph closure proven before deletion; publish.py imports cleanly after prune.
- 35 tests pass (rewrote the validator smoke test → keep-set import + "stay-deleted" guard;
  updated the dep-shape assertion). ruff + mypy clean.
- CPV `--strict` dry-run AND CI: CRITICAL=0 MAJOR=0 MINOR=0 NIT=0 (9 advisory WARNINGs).
- Published v1.3.3; Plugin Validation + Release CI green (commit 0fff792).

## Approval log

- 2026-06-11T14:54:14+0200 — Tier 0 (in-scope maintenance on THIS project). USER directed
  "bring the plugin up to date" and explicitly chose option 4 (prune) via AskUserQuestion;
  publish pre-authorized by that directive. Completed + shipped v1.3.3, CI green.
